// Example Morpho program
var x = 10
var list = [1, 2, 3]

fn factorial(n) {
    if (n <= 1) {
        return 1
    }
    return n * factorial(n - 1)
}

print factorial(5)

for (item in list) {
    print item * 2
}

class Foo {
    init(a,b,c) {
        self.a = a
        self.b = b
        self.c = c
    }
    oneMethod() {}
    anotherMethod() {}
}
